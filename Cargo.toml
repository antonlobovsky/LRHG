[package]
name = "hyperliquid_rust_bot"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
hyperliquid_rust_sdk = "0.6" # Use underscores for the crate name
ta = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
